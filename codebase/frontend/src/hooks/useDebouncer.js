// search input prevent too many API calls when a user is typing a search query.
// form validation delay validation until the user finishes typing.
//window resizing wait for the user to stop resizing the window before triggering an action.